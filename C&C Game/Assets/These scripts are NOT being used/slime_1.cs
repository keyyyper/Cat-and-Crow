using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class slime_1 : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        //timecode 42.51 in wyatt video shows slime code,,,, it is 2:35am,,,, i am going to bed lmao
    }

    // Update is called once per frame
    void Update()
    {
   //     slimeAnim.Play("Slime_idle");
    }

  //  public void Die()
  //  {
    //    Instantiate(DropPrefab).transform.position = transform.position;
        //play
    //    Destroy(gameObject);
 //   }

   // private void OnDrawGizmos()
 //   {
  //      Gizmos.DrawLine(transform.position, transform.position + Velocity.normalized * CheckDistance);
  //  }
}
